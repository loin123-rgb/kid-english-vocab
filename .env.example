# 本機跑 `netlify dev` 時會自動讀這個檔的環境變數。
# 用法:
#   1. 複製這個檔成 .env (`.env` 已在 .gitignore 內,不會 commit)
#   2. 把 DEEPL_API_KEY 後面填上你的 DeepL Free key (結尾是 :fx)
#   3. 安裝 netlify cli:  npm i -g netlify-cli
#   4. 跑:                netlify dev
#   5. 開 http://localhost:8888 (Netlify dev 會 proxy index.html + functions)
#
# 注意:這檔只給「本機開發」用。
# 部署到 Netlify 之後,production 還是要去
#   Netlify dashboard → Site configuration → Environment variables
# 設一份同名的 DEEPL_API_KEY,functions 才能讀到。

DEEPL_API_KEY=請貼上你的-deepl-key:fx
