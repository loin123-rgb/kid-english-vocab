# macOS / Windows
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Node
node_modules/
npm-debug.log*

# Misc
*.log
*.tmp

# Netlify CLI / 環境變數(絕對不能 commit)
.env
.env.*
!.env.example
.netlify/

# Cloudflare Wrangler 本機 cache(含 OAuth token,絕對不能 commit)
.wrangler/
wrangler.toml
wrangler.jsonc

# Python
__pycache__/
*.pyc

# 個人化 ComfyUI workflow(每人模型/設定不同,不該推共用)
data/raw/comfyui_workflow.json

# CSV 暫存 / 備份檔(批次跑壞時的歷史版本)
*.bak
*.v[0-9]*
*NOWFROZEN*
